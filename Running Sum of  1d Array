class Solution {
public:
    vector<int> runningSum(vector<int>& nums) {
        int i;
        int j=nums.size();
        int sum=0;
        for(i=0;i<j;i++)
        {
            sum=sum+nums[i];
            nums[i]=sum;
        }
        return nums;
    }
};
