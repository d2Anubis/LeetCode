class Solution {
public:
    int xorOperation(int n, int start) {
        int i;
        int st=start;
        int x=start;
        for(i=1;i<n;i++)
        {
            st=st+2;
            x=x^st;
        }
        return x;
    }
};
